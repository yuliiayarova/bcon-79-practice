## Створи список нотаток

На сторінці має бути форма з `input` та `textarea`, у які має вводитися назва та
текст нотатки. Після натискання на кнопку "Add note", нотатки додається до
списку `.js-notes-list`

У кожної нотатки має бути кнопка "Delete", щоб можна було прибрати нотатки зі
списку. Список із нотатками має бути доступним після перезавантаження сторінки.

### Розмітка нотатки

```html
<li class="notes-item">
  <button class="js-delete-btn" data-noteid="1">Delete</button>
  <h3>Назва нотатки</h3>
  <p>Текст (опис) нотатки</p>
</li>
```

### Завдання: Реалізація переліку нотаток

Базова HTML-структура створена в файлі index.html:

Є форма, в якій знаходяться:

Текстове поле (input) для введення назви нотатки.

Текстове поле (textarea) для введення тексту нотатки.

Кнопка "Add note" поруч із текстовими полями.

Пустий список `<ul id="notes-list">` для виведення нотаток.

Базове оформлення для списку нотаток, кнопок і полів повинно бути зроблено із
застосуванням
[Bootstrap](https://getbootstrap.com/docs/5.3/getting-started/introduction/).

### Реалізація функціоналу:

1. Доступ до всіх потрібних елементів
2. Додавання слухачів подій
3. Функція отримання даних для формування одного об'єкта нотатки.

### 1. Додавання нотаток:

Після натискання кнопки "Add", нотатка додається до списку. У кожній нотатці має
бути кнопка "Delete" для видалення нотатки. Очистка полів вводу після додавання
нотатки. Перевірка на заповненість полів (запобігання додаванню порожніх
нотаток).

### Видалення нотаток:

При натисканні на "Delete", відповідна нотатка має бути видалена зі списку.

### Збереження у localStorage:

Реалізуйте збереження списку нотаток у localStorage після кожного додавання чи
видалення. Завантажуйте збережені нотатки при перезавантаженні сторінки.

### Зміна теми

Реалізуйте зміну теми застосунку, використовуючи
[Dark mode](https://getbootstrap.com/docs/5.3/customize/color-modes/) з
бібліотеки Bootstrap

### Перевірка роботи застосунку:

- Нотатки успішно додаються до списку.
- Нотатки видаляються при натисканні кнопки "Delete".
- Список нотаток зберігається навіть після перезавантаження сторінки.

🚀 Готово! Тестуйте і переконайтесь, що все працює коректно.
